import { ArrowUpRight } from 'lucide-react';import { Link } from 'react-router-dom';import type { NewsArticle } from '../types/news';import { useLanguage } from '../i18n/useLanguage';import { LocalImage } from './Media';
export function NewsCard({article}:{article:NewsArticle}){const{language,t}=useLanguage();const zh=language==='zh';return <article className="group bg-white">{article.image&&<Link to={`/activity/${article.slug}`} className="block aspect-[16/10] overflow-hidden bg-graphite"><LocalImage loading="lazy" src={article.image} alt={zh?`${article.titleZh}活动图片`:`Cover image for ${article.titleEn}`} style={{objectPosition:article.imagePosition||'50% 50%',transform:`scale(${article.imageZoom||1})`}} className="size-full object-cover transition duration-500 group-hover:scale-105"/></Link>}<div className={`border border-line p-6 ${article.image?'border-t-0':''}`}><div className="flex justify-between text-xs font-semibold uppercase tracking-wide"><span className="text-accent">{zh?article.categoryZh:article.categoryEn}</span><time className="text-muted">{article.date}</time></div><h3 className="mt-4 text-xl font-bold leading-snug text-ink"><Link to={`/activity/${article.slug}`}>{zh?article.titleZh:article.titleEn}</Link></h3><p className="mt-3 text-sm leading-6 text-muted">{zh?article.summaryZh:article.summaryEn}</p><Link to={`/activity/${article.slug}`} className="mt-5 inline-flex items-center gap-1 text-sm font-semibold text-ink hover:text-accent">{t.common.readMore}<ArrowUpRight size={16}/></Link></div></article>}
