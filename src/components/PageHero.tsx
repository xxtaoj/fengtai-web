import { LocalImage } from './Media';
export function PageHero({image,eyebrow,title,description}:{image:string;eyebrow?:string;title:string;description:string}){return <section className="relative flex min-h-[31rem] items-end overflow-hidden bg-ink pb-20 pt-40 text-white"><LocalImage src={image} alt={title} className="absolute inset-0 size-full object-cover opacity-45"/><div className="absolute inset-0 bg-gradient-to-r from-ink via-ink/70 to-transparent"/><div className="industrial-grid absolute inset-0"/><div className="container-shell relative"><p className="mb-5 text-xs font-bold uppercase tracking-[.24em] text-amber-400">{eyebrow}</p><h1 className="max-w-4xl text-4xl font-bold leading-tight tracking-tight md:text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-200">{description}</p></div></section>}
