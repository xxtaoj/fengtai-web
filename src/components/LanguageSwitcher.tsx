import { Languages } from 'lucide-react'; import { useLanguage } from '../i18n/useLanguage';
export function LanguageSwitcher({dark=false}:{dark?:boolean}){const{language,setLanguage}=useLanguage();return <div className={`flex items-center gap-1 text-xs font-semibold ${dark?'text-white':'text-body'}`} aria-label="Language selector"><Languages size={16} aria-hidden="true"/><button onClick={()=>setLanguage('zh')} className={`min-h-11 px-2 ${language==='zh'?'text-accent underline underline-offset-4':''}`} aria-pressed={language==='zh'}>中文</button><span aria-hidden="true" className="opacity-40">|</span><button onClick={()=>setLanguage('en')} className={`min-h-11 px-2 ${language==='en'?'text-accent underline underline-offset-4':''}`} aria-pressed={language==='en'}>English</button></div>}
