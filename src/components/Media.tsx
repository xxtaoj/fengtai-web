import { useState, type ImgHTMLAttributes } from 'react'; import { ImageOff } from 'lucide-react'; import { useLanguage } from '../i18n/useLanguage';
export function LocalImage({className='',...props}:ImgHTMLAttributes<HTMLImageElement>){const[failed,setFailed]=useState(false);const{language}=useLanguage();if(failed)return <div className={`media-placeholder flex min-h-48 items-center justify-center p-6 text-center text-sm text-white/70 ${className}`} role="img" aria-label={props.alt}><div><ImageOff className="mx-auto mb-3"/><span>{language==='zh'?'本地图片待替换':'Replace local image'}<br/><code className="text-xs">{props.src}</code></span></div></div>;return <img {...props} className={className} onError={()=>setFailed(true)}/>}
