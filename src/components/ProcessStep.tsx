export function ProcessStep({number,title,description}:{number:number;title:string;description?:string}){return <article className="relative border-t border-line pt-6"><span className="text-xs font-bold tracking-[.2em] text-accent">{String(number).padStart(2,'0')}</span><h3 className="mt-3 font-bold text-ink">{title}</h3>{description&&<p className="mt-2 text-sm leading-6 text-muted">{description}</p>}</article>}
