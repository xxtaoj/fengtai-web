import { ArrowUpRight } from 'lucide-react';import { Link } from 'react-router-dom';import type { Product } from '../types/product';import { useLanguage } from '../i18n/useLanguage';import { LocalImage } from './Media';
export function ProductCard({product}:{product:Product}){const{language,t}=useLanguage();const zh=language==='zh';return <article className="group overflow-hidden border border-line bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lift"><Link to={`/products/${product.slug}`} className="block aspect-[4/3] overflow-hidden"><LocalImage src={product.image} alt={zh?`${product.nameZh}产品图片`:`${product.nameEn} product image`} className="size-full object-cover transition duration-500 group-hover:scale-105"/></Link><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-accent">{zh?product.categoryZh:product.categoryEn}</p><h3 className="mt-3 text-xl font-bold text-ink">{zh?product.nameZh:product.nameEn}</h3><p className="mt-2 text-sm leading-6 text-muted">{zh?product.summaryZh:product.summaryEn}</p><div className="mt-5 flex items-center justify-between border-t border-line pt-4 text-sm font-semibold"><Link to={`/products/${product.slug}`} className="flex items-center gap-1 text-ink hover:text-accent">{t.common.viewDetails}<ArrowUpRight size={16}/></Link><Link to="/contact#inquiry" className="text-accent">{t.common.quote}</Link></div></div></article>}
