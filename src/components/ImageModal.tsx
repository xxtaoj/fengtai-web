import { useEffect } from 'react';import { X } from 'lucide-react';import { LocalImage } from './Media';
export function ImageModal({src,alt,onClose}:{src:string;alt:string;onClose:()=>void}){useEffect(()=>{const f=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[onClose]);return <div className="fixed inset-0 z-[80] grid place-items-center bg-ink/90 p-5" role="dialog" aria-modal="true" aria-label={alt}><button onClick={onClose} className="absolute right-5 top-5 grid size-11 place-items-center text-white" aria-label="Close image"><X/></button><LocalImage src={src} alt={alt} className="max-h-[86vh] max-w-full object-contain"/></div>}
