import { useState } from 'react';import { Play } from 'lucide-react';import { useReducedMotion } from '../hooks/useReducedMotion';import { LocalImage } from './Media';
export function VideoBlock({src,poster,title,autoPlay=false}:{src:string;poster:string;title:string;autoPlay?:boolean}){const reduced=useReducedMotion();const[failed,setFailed]=useState(false);if(reduced||failed)return <div className="relative aspect-video overflow-hidden bg-ink"><LocalImage src={poster} alt={`${title} poster`} className="size-full object-cover opacity-70"/><div className="absolute inset-0 grid place-items-center text-center text-white"><Play size={42}/><span className="mt-2 text-sm">{title}</span></div></div>;return <video src={src} poster={poster} className="aspect-video w-full object-cover bg-ink" autoPlay={autoPlay} muted={autoPlay} loop={autoPlay} playsInline controls={!autoPlay} onError={()=>setFailed(true)}>Your browser does not support video playback.</video>}
