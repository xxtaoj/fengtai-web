import { Link } from 'react-router-dom';import { ArrowLeft } from 'lucide-react';import { useLanguage } from '../i18n/useLanguage';import { Seo } from '../components/Seo';
export function NotFoundPage(){const{language}=useLanguage();const zh=language==='zh';return <main className="industrial-grid grid min-h-screen place-items-center bg-ink p-6 text-center text-white"><Seo title={{zh:'页面未找到',en:'Page Not Found'}} description={{zh:'页面不存在。',en:'The page does not exist.'}}/><div><p className="text-8xl font-bold text-amber-400">404</p><h1 className="mt-5 text-3xl font-bold">{zh?'页面未找到':'Page Not Found'}</h1><p className="mt-3 text-slate-400">{zh?'链接可能已失效，或页面已被移动。':'The link may be invalid or the page has moved.'}</p><Link to="/" className="mt-8 inline-flex min-h-12 items-center gap-2 bg-accent px-6 font-semibold"><ArrowLeft/>{zh?'返回首页':'Back to Home'}</Link></div></main>}
